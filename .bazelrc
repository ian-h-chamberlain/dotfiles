# set up environment variables for pyenv to work properly
build --action_env=PATH
build --distinct_host_configuration=false
build --action_env=PYENV_VERSION=3.6.5:2.7.15

# remote caching in the lab
# build --remote_http_cache=http://172.20.6.206:9090
# build --remote_timeout=5

# and make sure tests run with proper pyenv versions as well
test --test_env=PYENV_VERSION=3.6.5:2.7.15
# test --test_arg=-vvv
