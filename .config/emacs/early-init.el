;; TODO: may want to move other "dir" customization into here as well,
;; but it seems like package-user-dir is the important one for now
(custom-set-variables
 '(package-user-dir "~/.local/share/emacs/elpa"))
